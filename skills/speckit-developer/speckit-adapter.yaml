name: speckit-developer
hooks:
  - phase: implement
    priority: 100
    context: SKILL.md
    instructions: |
      ## SpecKit Integration
      - Follow Constitution Article IV: Test-First Imperative (Red-Green-Refactor)
      - Stop at `<!-- CONVERGENCE_BOUNDARY -->` marker — do not execute Phase N tasks
      - After Phase N-1 completion, prompt user to run `/speckit.converge`
  - phase: tasks
    priority: 90
    context: SKILL.md
    instructions: |
      ## SpecKit Integration
      - Ensure every implementation task has a preceding test task (TDD pairing)
      - Use `[Skill: speckit-developer]` label for implementation tasks
